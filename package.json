{
  "name": "kurlyro-test",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --turbopack",
    "pre-run": "node deeplink.js",
    "build": "npm run pre-run && next build",
    "start": "npm run pre-run && next start",
    "lint": "next lint"
  },
  "dependencies": {
    "next": "15.2.4",
    "nvm": "^0.0.4",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  }
}
